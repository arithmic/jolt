use std::time::Instant;

pub fn main() {
    let target_dir = "/tmp/jolt-guest-targets";
   // // Could be used to set the target directory for the guest program
   // // unless  #[jolt::provable] is uncommented in the guest code
    // let mut program = guest::compile_verify(target_dir);
    // println!("Program compiled successfully");
}
